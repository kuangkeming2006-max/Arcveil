package mcoverlay;

// Separate versioned bridge: a previously loaded NativeLogicalBridge_v32
// must never be reused with a different native method table.
public final class NativeSprintBridge_v53 {
    public static native void headingPre(Object entity);
    public static native void headingPost(Object entity);
}
